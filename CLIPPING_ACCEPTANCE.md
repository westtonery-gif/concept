# CLIPPING_ACCEPTANCE.md — Критерии приёмки департамента нарезки

> Приёмка `CLIPPING_SPEC.md` (по `ADR-0053`/`0055`/`0056`/`0058`/`0059`). Каждый критерий
> фальсифицируем: при неверной реализации соответствующий тест падает. Идентификаторы
> используются в именах тестов.
>
> **Статус:** Accepted. **Дата:** 2026-09-20.

---

## Состояние реализации

Департамент закрывается слоями, и часть из них упирается во внешнее, чего в этой среде нет.
Таблица честно говорит, что уже проверяемо, а что ждёт — чтобы «не реализовано» не читалось как
«забыто».

| Слой | Критерии | Состояние |
|---|---|---|
| `EpisodeBoard` + `IncomingEpisode` + `ClipMode` | EPB | реализуется первым |
| `NotionEpisodeBoard` | NEB | реализуется первым (живой Notion доступен) |
| `EpisodeSource` (локальный файл) | ESR | ждёт файла эпизода от владельца продукта |
| `FootageIndex` | FIX | ждёт аккаунта Vyra AI; порт и заглушка — раньше |
| `plan_clips` | CLP | чистая функция, внешнего не требует |
| `ClipRenderer` + `check_clip_format` | RND | **ffmpeg в среде не установлен**; чистая проверка формата — раньше адаптера |
| `clip_qa_agent@v1` | CQA | роль и Schema — раньше; живой вызов модели — прогон оператора |
| Оркестрация (один Run на серию) | CRN | после того, как слои выше существуют |

## 1. Борд эпизодов (EPB) — `tests/test_episode_board.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| EPB-01 | `IncomingEpisode` с пустым `episode_ref` / `source_ref`; `mode` не `ClipMode` | `ValueError`, названо поле |
| EPB-02 | `IncomingEpisode` корректен | иммутабелен (frozen, `__slots__`), поля читаются |
| EPB-03 | `ClipMode` | ровно два значения, `CHUNK` и `SCENE`; `SEMANTIC`/`BOTH` отсутствуют (`ADR-0058` §1) |
| EPB-04 | `EpisodeBoard` — `Protocol`; `EpisodeBoardError` | структурное соответствие проверяется mypy; ошибка **не** `DomainError` |
| EPB-05 | границы слоя | `adapters/episode_board.py` импортирует только stdlib и `domain.*` — как весь `adapters/` |

## 2. Заглушка борда (STE) — `tests/test_in_memory_adapters.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| STE-01 | `put` (сторона редактора, вне `Protocol`) → `fetch_episode` | возвращается положенный эпизод |
| STE-02 | неизвестный / не готовый эпизод | `None` |
| STE-03 | `report_status` по неизвестному эпизоду | `EpisodeBoardError` (fail loud, как `ADR-0025` §3) |
| STE-04 | повторный `report_status` с тем же статусом | безвреден; прочитывается, что видит внешняя сторона |

## 3. Notion-борд эпизодов (NEB) — `tests/test_notion_episode_board.py`

Настоящий код адаптера (`urllib`) ходит через реальный сокет к локальному серверу, играющему
Notion, — как `NBB` и `NRD`. Ниже адаптера ничего не подменяется.

| ID | Сценарий | Ожидание |
|---|---|---|
| NEB-01 | готовая карточка эпизода | `IncomingEpisode` с `source_ref` и `mode` из свойств |
| NEB-02 | не готова / архивна / из другой базы / без `source_ref` | `None` — неразличимо |
| NEB-03 | свойство режима держит значение вне `ClipMode` | `EpisodeBoardError` — криво настроенная база, **не** молчание |
| NEB-04 | `report_status` | два `rich_text`-свойства перезаписаны; повтор безвреден |
| NEB-05 | карточка не на борде | отказ **до** любого `PATCH` |
| NEB-06 | нет свойства / неверный тип; не-2xx; сеть; не JSON | `EpisodeBoardError` |
| NEB-07 | любой запрос | несёт `Authorization: Bearer <токен>` и закреплённый `Notion-Version` |
| NEB-08 | настройки из окружения | все обязательные `OMEMO_EPISODE_NOTION_*`; missing названы; токена нет в `repr` и сообщениях |

## 4. План клипов (CLP) — `tests/test_clip_plan.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| CLP-01 | `CHUNK`, эпизод 25 мин, `chunk_ms` 2 мин | подряд идущие интервалы, не пересекаются, покрывают эпизод, `index` с 1 |
| CLP-02 | `CHUNK`, граница попадает в середину реплики | сдвинута к ближайшей паузе в пределах допуска |
| CLP-03 | `CHUNK`, паузы рядом нет | граница остаётся на месте — правило не ломает план |
| CLP-04 | `SCENE`, короткие подряд идущие сцены | слиты, пока сумма `<= max_ms` |
| CLP-05 | `SCENE`, сцена длиннее `max_ms` | разрезана по правилу `CHUNK` |
| CLP-06 | `SCENE`, сцен нет | план пуст; это не ошибка |
| CLP-07 | эпизод короче куска | ровно один клип на весь эпизод |
| CLP-08 | `transcript` каждого клипа | текст речи ровно его интервала, в порядке |
| CLP-09 | чистота | без ввода-вывода, часов и случайности (скан импортов, как `TLB-01`) |

## 5. Формат клипа (RND) — `tests/test_clip_format.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| RND-01 | измерения в пределах лимитов | нарушений нет |
| RND-02 | длительность больше лимита платформы | одно нарушение, названа длительность |
| RND-03 | контейнер вне списка | нарушение |
| RND-04 | соотношение сторон **не** проверяется | 16:9 нарушением не является (`ADR-0058`: платформа сама добавит поля) |
| RND-05 | нарушение формата | ведёт к `FAILED` Task со стабильной причиной, **не** к `flagged`-вердикту (`ADR-0056` §1) |

## 6. QA клипа (CQA) — `tests/test_clip_qa_agent.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| CQA-01 | `clip_qa_agent@v1` | Agent → Prompt `clip-qa-agent` → Schema `qa-verdict@v1`; `skill_refs` и `tool_refs` пусты |
| CQA-02 | схема вердикта | **та же** `qa-verdict@v1`; второго словаря вердиктов нет (`ADR-0034`) |
| CQA-03 | Prompt v1 | несёт ровно три критерия; «не резать посреди слова» в нём **нет** |
| CQA-04 | вход оценщика | содержит расшифровку клипа **и** расшифровку всей серии (`ADR-0056` §3) |
| CQA-05 | роль как шаг Workflow | отвергается, как `qa_agent` (`ADR-0038`) |

## 7. Оркестрация (CRN) — `tests/test_clip_production.py`

| ID | Сценарий | Ожидание |
|---|---|---|
| CRN-01 | серия с N клипами | один Run; N Task'ов, N Output'ов, N артефактов; статус Run `waiting_qa` → `waiting_human` |
| CRN-02 | у каждого клипа | своя `Evaluation` и своё `HumanReview` на **его** `artifact_id` |
| CRN-03 | один клип `flagged`, остальные `passed` | одобренные доходят до `APPROVED`; `flagged` не одобряется; Run **не** падает (`ADR-0059` §3) |
| CRN-04 | клип отклонён человеком | не шипится; цепочки `SUPERSEDED` и доработки **нет** (`ADR-0059` §6) |
| CRN-05 | все клипы получили терминальное решение | Run `COMPLETED`, каким бы ни был состав решений |
| CRN-06 | публикация ревью | адресует **каждый** артефакт, а не `pending[-1]`; двенадцать из тринадцати не теряются (`ADR-0059` §5) |
| CRN-07 | существующие `pending_review` / `publish_pending_review` / `take_review_decision` | поведение **не изменено** — приёмка Этапов 10–12 остаётся зелёной |
| CRN-08 | падение после **каждой** точки коммита | возобновление не дублирует закоммиченный Task и не пере-записывает вердикт (дисциплина `ADR-0026` §2, форма `SWR`) |
| CRN-09 | эпизод не готов / не найден | Run не создаётся, ничего не сохранено |
| CRN-10 | `ContentDirector` | **не изменён**: департамент не проходит через него (`ADR-0059` §4) |
